# 🔥 วิธีแก้ปัญหา "ไม่พบข้อมูล" ใน Firebase

## สาเหตุ

จากภาพที่คุณส่งมา ข้อมูลใน Firebase ถูกสร้างด้วย **Auto-generated Document ID** (`nmGKXq8YiSVcceB3lCx2`) แต่โค้ดใน `Register.jsx` ปัจจุบันใช้ `line_user_id` เป็น Document ID โดยตรง

ทำให้เกิดความไม่ตรงกันระหว่าง:

- **ข้อมูลเก่า**: Document ID = `nmGKXq8YiSVcceB3lCx2`, field `line_user_id` = `"U8d4cf39c931%c5ddd5a%9b35516056e4"`
- **โค้ดใหม่**: พยายามหา Document ID = `"U8d4cf39c931%c5ddd5a%9b35516056e4"` โดยตรง

## วิธีแก้ไข (เลือก 1 ใน 2)

### ✅ วิธีที่ 1: ลบข้อมูลเก่าแล้วลงทะเบียนใหม่ (แนะนำ)

1. เปิด [Firebase Console](https://console.firebase.google.com/project/internal-wallet-58a3c/firestore)
2. ไปที่ Collection `profiles`
3. ลบ Document `nmGKXq8YiSVcceB3lCx2`
4. รีเฟรชแอปแล้วลงทะเบียนใหม่
5. ระบบจะสร้าง Document ใหม่โดยใช้ `line_user_id` เป็น Document ID

**ข้อดี**: เร็ว แม่นยำ ไม่ต้องแก้โค้ด

### ⚙️ วิธีที่ 2: ลบข้อมูลเก่าใน Firebase Console แล้วลงทะเบียนใหม่

ถ้าคุณต้องการให้ระบบรองรับทั้งข้อมูลเก่าและใหม่ ให้ทำตามนี้:

1. ไปที่ Firebase Console > Firestore Database
2. เลือก Document `nmGKXq8YiSVcceB3lCx2`
3. คลิก "Delete document"
4. รีเฟรชแอป
5. ลงทะเบียนใหม่ (ระบบจะสร้าง Document ด้วย line_user_id เป็น ID)

## ตรวจสอบว่าแก้ไขสำเร็จ

เปิด Browser Console (F12) แล้วดูว่า:

- มีข้อความ `✅ พบข้อมูล (ใช้ Document ID)!` = สำเร็จ!
- หรือ `✅ พบข้อมูล (จากการค้นหาทั้งหมด)!` = สำเร็จ (แต่ช้ากว่า)
